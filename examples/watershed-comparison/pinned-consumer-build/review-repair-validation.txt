Independent review repair evidence

Observed-manifest regression: actual native supervised fixture with WRONG-FABRIC,
WRONG-VERSION and all-zero manifest checksum succeeded before fix (DID NOT RAISE).
It now fails before Engine construction and preserves observed raw manifest bytes.
The API cannot inject observed manifest bytes into Engine or pin object versions;
matching before/after observations are explicit temporal evidence, not an immutable
session snapshot. No remote dataset was opened for these tests.

Installed-facade regression: appended bytes to actual installed Python facade,
restored in finally; original verifier accepted it, corrected verifier rejects.
Loaded-library inventory regression: injected empty dyld image inventory into
actual consumer identity verification; original verifier ignored it, corrected
verifier rejects. Native success tests verify actual full loaded paths/hashes.
Runtime-data checksum regression: forged supplemental receipt accepted before
runtime-data binding, rejected after. Supplemental receipt conservatively records
GDAL anchor and full existing data-directory inventories, ordered PROJ candidate
paths and disabled network. Still-absent first per-user candidate may relocate
under isolated HOME; populated/other paths fail closed.

Supervisor signal regression: SIGTERM to real supervisor left real sleeping
worker alive before fix; corrected supervisor records cancellation, stops group
and reaps. Test forcibly killed residual regression child in cleanup. Descendant
regression: SIGTERM-ignoring grandchild survived leader exit before fix; corrected
whole-group bounded escalation terminates it. No native/remote sessions involved.

Composed storage receipt regressions used the actual source-produced synthetic
receipt retained in tests/fixtures/verified-delivery.json. Missing README,
extraneous object, wrong total, wrong verification method and nonverified object
all passed old gate, all now rejected. Top-level current status must be verified;
historical success cannot override refused/checking state.

Overlay regressions exercised actual Cartopy rendering before fixes: failed or
missing-success output, substituted matching geometry pair, metadata/traversal
mismatch, parent cancellation, missing/empty/changed trace, and changed runtime
identity all previously produced plots. Checks now bind the complete success,
request, metadata, geometry, traversal, manifest, trace and runtime evidence chain.

Combined command in scripts/watershed-comparison:
HFX_TEST_CONSUMER_IDENTITY=<comparison-evidence>/consumer-identity-v2.json HFX_TEST_LOCAL_FIXTURE=<pinned source>/crates/core/tests/fixtures/parity/v021_synthetic_refined uv run --frozen --no-sync pytest -q --basetemp <comparison-evidence>/review-acceptance-tests
Observed result: exit 0; 72 passed in 10.84s; 56 upstream Cartopy/Matplotlib deprecation warnings.
Ruff check/format completed successfully. Later final runtime-required-field test
results are reported in the PR update. These results are offline execution proof,
not real TDX/GRIT Basel delineation or delivered payload proof.

Final combined rerun after mandatory runtime bindings and historical-refusal test:
--basetemp <comparison-evidence>/final-review-repair-tests
exit 0; 75 passed in 10.85s; 56 upstream Cartopy/Matplotlib deprecation warnings.
No signal fault probe left a running process. Required command handles completed.

Bytecode import repair:
An isolated copy of the exact wheel retained unchanged verified __init__.py but
contained an unchecked-hash compiled cache with a marker-writing payload. The
actual supervised native worker previously succeeded and executed the marker;
red regression failed its no-marker assertion. After fresh per-worker
sys.pycache_prefix setup, the same worker succeeds without executing stale cache.
The supervisor also configures a fresh startup PYTHONPYCACHEPREFIX. Pre-imported
consumer modules are refused. -B alone was not used as a read-isolation guarantee.
No shared installed package was changed by this regression. The earlier facade
integrity test now also uses an isolated wheel copy to avoid shared-state mutation.
Final bytecode full-suite rerun: 76 passed in 16.33s; 56 upstream deprecation
warnings; then added preimported-consumer gate test passed independently (1 passed).
Ruff check/format passed. All package copies are below hfx/.worktrees test evidence.
