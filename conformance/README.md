# Conformance

This directory is reserved for validator and interoperability fixtures.

- [`valid/`](./valid) should contain datasets expected to pass validation.
- [`invalid/`](./invalid) should contain intentionally broken datasets expected to fail validation.

Keep these fixtures focused on contract coverage rather than realism or dataset size.
